import style from "./Cart.module.css";

export const Cart = (): JSX.Element => {
  return (
    <main className={style.main}>
      <section className={style.content}>Cart</section>
    </main>
  );
};
